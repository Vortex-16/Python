name="Vikash"

print(len(name))

print(name.endswith("sh"))
print(name.startswith("Vi"))
print(name.capitalize())
# print(name.replace("Vik","Div"))

#Other Functions in String are:
# isalnum() - Returns True if all characters in the string are alphanumeric
# isalpha() - Returns True if all characters in the string are alphabetic
# isdigit() - Returns True if all characters in the string are digits
# islower() - Returns True if all characters in the string are lowercase
# isupper() - Returns True if all characters in the string are uppercase
# istitle() - Returns True if the string is a titlecased string
# isspace() - Returns True if all characters in the string are whitespace
# isprintable() - Returns True if all characters in the string are printable
# join() - Joins the elements of an iterable with a specified separator
# split() - Splits the string into a list of substrings based on a specified separator
# splitlines() - Splits the string into a list of lines
# find() - Returns the lowest index of the substring if found in the string, otherwise returns -1
# rfind() - Returns the highest index of the substring if found in the string, otherwise returns -1
# count() - Returns the number of occurrences of a substring in the string
# format() - Formats the string using placeholders
# format_map() - Formats the string using a mapping (dictionary)
# strip() - Removes leading and trailing whitespace from the string
# lstrip() - Removes leading whitespace from the string
# rstrip() - Removes trailing whitespace from the string
# title() - Converts the first character of each word to uppercase and the rest to lowercase
# swapcase() - Swaps the case of all characters in the string
# zfill() - Pads the string with zeros on the left to fill a specified width
# center() - Centers the string within a specified width, padding with spaces
# ljust() - Left-justifies the string within a specified width, padding with spaces
# rjust() - Right-justifies the string within a specified width, padding with spaces
# translate() - Translates characters in the string using a translation table
# maketrans() - Creates a translation table for use with the translate() method
# swapcase() - Swaps the case of all characters in the string
# casefold() - Converts the string to lowercase, more aggressive than lower()
# format() - Formats the string using placeholders
# format_map() - Formats the string using a mapping (dictionary)
# partition() - Splits the string into three parts: the part before the separator, the separator itself, and the part after the separator
# rpartition() - Splits the string into three parts: the part before the last occurrence of the separator, the separator itself, and the part after the separator
# rsplit() - Splits the string into a list of substrings from the right side based on a specified separator
# rsplit() - Splits the string into a list of substrings from the right side based on a specified separator
# casefold() - Converts the string to lowercase, more aggressive than lower()
# translate() - Translates characters in the string using a translation table
# maketrans() - Creates a translation table for use with the translate() method
# swapcase() - Swaps the case of all characters in the string
# casefold() - Converts the string to lowercase, more aggressive than lower()